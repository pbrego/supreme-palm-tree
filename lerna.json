{
  "command": {
    "publish": {
      "private": false,
      "conventionalCommits": true,
      "message": "chore(release): publish",
      "ignoreChanges": ["**/*.test.*", "**/*.md"]
    }
  },
  "packages": ["clients/**", "configs/**", "docs/**", "libs/**", "tools/**"],
  "version": "independent",
  "useWorkspaces": true,
  "npmClient": "yarn"
}
